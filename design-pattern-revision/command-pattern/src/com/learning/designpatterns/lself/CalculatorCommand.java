package com.learning.designpatterns.lself;
//command says there could be any number of arguments that can be passed
//let that be handled in command itself
public interface CalculatorCommand {

	void execute();
}
